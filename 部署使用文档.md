# 蚁仓 Ant Rack System (ANS) V1.0 — 部署使用文档

> 单人管理员模式 · 库存只读 · 流水驱动 · 加权算价 · 双端适配 · Docker 一键部署

---

## 一、系统概述

本系统为个人 DIY 工作室 / 小型研发作坊设计的轻量化物料库存管理系统，仅保留「物料管理 + 研发项目 BOM + 库存成本记账」核心场景，无采购/销售/工单等工业级冗余模块。

**核心铁规：**
1. 库存数据只读，仅流水驱动变更
2. 入库加权算价、出库锁死单价
3. 二级固定分类、无多级嵌套
4. 项目三状态锁定/消耗/解锁闭环
5. 所有计算后端完成、前端只展示
6. 全套事务包裹，杜绝数据错乱

**默认管理员：** `admin` / `admin123`（首次登录后请及时修改）

---

## 二、环境依赖

### Docker 部署（推荐，最简）
- Docker 20.10+
- Docker Compose v2（`docker compose` 命令）

### 本地开发运行
**后端：**
- Python 3.10+
- 依赖见 `backend/requirements.txt`

**前端：**
- Node.js 18+
- npm 或 pnpm

---

## 三、Docker 一键部署（生产推荐）

适配 NAS、AIO 主机 7×24 小时挂机运行。

### 1. 部署步骤

```bash
# 进入项目根目录
cd d:\TK01_AntRack

# 一键构建并启动前后端
docker compose up -d --build
```

### 2. 访问系统

启动完成后，浏览器访问：`http://<服务器IP>:8080`

使用默认账号 `admin` / `admin123` 登录。

### 3. 端口与数据持久化

| 项目 | 说明 |
|------|------|
| 宿主机端口 | `8080` → 容器 `80`（前端），在 `docker-compose.yml` 的 `ports` 修改 |
| 数据库文件 | 挂载 `./backend/data` → 容器 `/app/data`（SQLite 文件 `stock.db`） |
| 上传图片 | 挂载 `./backend/static` → 容器 `/app/static` |

> 容器重启、重建均不丢失数据，所有数据保存在宿主机挂载目录。

### 4. 常用运维命令

```bash
# 查看服务状态
docker compose ps

# 查看实时日志
docker compose logs -f

# 重启服务
docker compose restart

# 停止服务
docker compose down

# 重新构建（更新代码后）
docker compose up -d --build
```

---

## 四、本地开发运行

### 后端启动

```bash
cd backend
python -m pip install -r requirements.txt
python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

首次启动会自动：
- 创建全部 6 张数据表
- 创建默认管理员 `admin/admin123`
- 写入系统默认二级分类（电子元器件/五金结构件/工具设备/耗材辅料）

后端 API 文档（Swagger）：`http://localhost:8000/docs`

### 前端启动

```bash
cd frontend
npm install
npm run dev
```

前端开发服务：`http://localhost:5173`（已配置代理转发 `/api` 与 `/static` 到后端 8000 端口）

### 前端打包

```bash
cd frontend
npm run build
```

产物输出到 `frontend/dist`，可由任意静态服务器或 Nginx 托管。

---

## 五、数据备份与迁移

### 备份

SQLite 单文件数据库，**直接复制文件即完成备份**：

```bash
# 停止服务避免写入
docker compose stop

# 备份数据库与图片
cp -r ./backend/data ./backup_data_$(date +%Y%m%d)
cp -r ./backend/static ./backup_static_$(date +%Y%m%d)

# 重新启动
docker compose start
```

### 迁移到新机器

1. 拷贝整个项目目录（含 `backend/data` 与 `backend/static`）到新机器
2. 执行 `docker compose up -d --build`
3. 数据完整恢复

### 数据导出（系统内）

系统内置 Excel 导出功能，可在以下页面使用：
- 物料列表 → 批量导出（物料台账）
- 库存流水 → 导出流水
- 项目列表 → 批量导出 / 单项目导出 BOM

---

## 六、管理员密码修改

**方式一：系统内修改（推荐）**

登录系统 → 左侧菜单「系统设置」→ 修改管理员密码 → 填写原密码与新密码 → 确认修改 → 重新登录。

**方式二：命令行重置（忘记密码时）**

```bash
# 进入后端容器
docker compose exec backend python -c "
from database import SessionLocal
from core.security import hash_password
from models.user import User
db = SessionLocal()
u = db.query(User).filter(User.username=='admin').first()
u.password_hash = hash_password('新密码')
db.commit()
print('密码已重置')
"
```

---

## 七、核心业务操作流程

### 1. 物料入库（加权算价）
物料列表 → 选择物料 → 入库 → 填写入库数量 + 券后实付总价 → 系统自动计算新加权单价。

> 多批次入库会自动加权平均，适配优惠券、满减、凑单场景。

### 2. 临时出库（锁死单价）
物料列表 → 选择物料 → 临时出库 → 填写出库数量 → 扣减库存与成本，**加权单价永久不变**。

### 3. 项目物料锁定
项目详情 → 新增 BOM → 选择物料 + 预估用量 → 点击「锁定」→ 输入锁定数量。
- 锁定仅占用可用库存，**不改动真实库存、成本、单价**
- 可用库存 = 总库存 - 锁定数量

### 4. 项目三状态闭环
- **准备阶段**：新增 BOM、锁定物料
- **制作阶段**：微调预估用量、确认部分消耗（仅计数，真实出库在完工时执行）
- **完工结算**：已消耗物料自动出库扣库存与成本 + 未消耗锁定物料自动解锁归还可用库存 + 项目归档不可再改 BOM

### 5. 数据导出
物料台账、库存流水、项目详情、BOM 清单均可一键导出 Excel。

---

## 八、常见问题排查

### Q1: 启动后访问 8080 端口打不开？
- 检查容器状态：`docker compose ps`，确认 frontend 与 backend 均 `running`
- 检查防火墙是否放行 8080 端口
- 查看日志：`docker compose logs backend` / `docker compose logs frontend`

### Q2: 登录提示「账号或密码错误」？
- 默认账号 `admin` / `admin123`（注意全小写）
- 若已修改密码且遗忘，按「六、管理员密码修改」方式二重置

### Q3: 上传图片失败？
- 检查 `./backend/static` 目录是否存在且有写权限
- 图片限制：png/jpg/jpeg/gif/webp/bmp，单文件 ≤ 5MB

### Q4: 如何修改对外端口？
编辑 `docker-compose.yml`，修改 frontend 服务的 `ports`：
```yaml
ports:
  - "9000:80"   # 改为 9000
```
然后 `docker compose up -d`。

### Q5: 如何公网访问？
- 将 8080 端口通过路由器端口映射或内网穿透暴露到公网
- 建议在公网入口加 HTTPS 反向代理（如 Nginx/Caddy）保障密码传输安全
- 系统已内置全局登录拦截，未登录无法访问任何业务接口

### Q6: passlib/bcrypt 启动报错？
若出现 `password cannot be longer than 72 bytes` 等错误，是 passlib 1.7.4 与 bcrypt 4.1+ 不兼容。本项目已固定 `bcrypt==4.0.1`，请确认 `requirements.txt` 未被修改。

### Q7: 数据库文件在哪？能否直接查看？
- 路径：`backend/data/stock.db`（SQLite 单文件）
- 可用 DB Browser for SQLite、DBeaver 等工具直接打开查看
- ⚠️ 请勿手动修改库存字段（stock_total_num/stock_total_cost/stock_avg_price/lock_num），仅通过系统出入库操作变更

---

## 九、工程目录结构

```
TK01_AntRack/
├── docker-compose.yml          # 一键编排
├── 部署使用文档.md
├── backend/                    # 后端 FastAPI
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── main.py                 # 入口
│   ├── config.py               # 配置
│   ├── database.py             # 引擎/会话
│   ├── dependencies.py         # 鉴权依赖
│   ├── init_db.py              # 自动建表+初始化
│   ├── core/
│   │   ├── stock_calc.py       # 加权算价/出库锁价算法（铁规核心）
│   │   ├── security.py         # BCrypt+JWT
│   │   └── response.py         # 统一响应
│   ├── models/                 # 6张表 SQLAlchemy Model
│   ├── schemas/                # Pydantic 入参出参
│   ├── routers/                # 路由（auth/dashboard/category/material/project/stock_log/export）
│   ├── data/                   # SQLite 数据库（挂载）
│   └── static/                 # 上传图片（挂载）
└── frontend/                   # 前端 Vue3 + Element Plus
    ├── Dockerfile
    ├── nginx.conf
    ├── package.json
    ├── vite.config.js
    ├── index.html
    └── src/
        ├── main.js
        ├── App.vue
        ├── router/             # 路由+登录拦截
        ├── api/                # Axios 封装
        ├── styles/             # 深色科技主题
        ├── layout/             # 主布局+侧边栏
        ├── components/         # 物料/入库/出库弹窗
        └── views/              # 11个业务页面
```

---

## 十、技术栈清单

| 层 | 技术 |
|----|------|
| 后端框架 | FastAPI 0.110 + Uvicorn |
| ORM | SQLAlchemy 2.0（同步） |
| 数据库 | SQLite 单文件 |
| 鉴权 | python-jose (JWT) + passlib (BCrypt) |
| 导出 | pandas + xlsxwriter |
| 前端框架 | Vue3 组合式 API + Vite |
| UI 组件 | Element Plus |
| 请求 | Axios |
| 部署 | Docker + Docker Compose + Nginx |

---

**V1.0 交付完成。** 系统已通过完整业务闭环测试：登录→入库加权算价→出库锁价→项目锁定→部分消耗→完工结算自动出库+解锁→流水全追溯，核心铁规全部验证通过。
